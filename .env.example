# ===========================================
# Convex Configuration
# ===========================================

# Your Convex deployment URL (required)
# Get this from your Convex dashboard: https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Convex site URL for authentication (required for auth features)
# This is the HTTP Actions URL from your Convex dashboard
CONVEX_SITE_URL=https://your-deployment.convex.site

# ===========================================
# App Configuration
# ===========================================

# Base path for the app (optional)
# Set this if deploying to a sub-path like /ui
# Leave empty or remove for root deployment
NEXT_PUBLIC_BASE_PATH=

# App URL for generating absolute URLs (optional)
NEXT_PUBLIC_APP_URL=http://localhost:3004

# ===========================================
# Vercel-specific (auto-populated by Vercel)
# ===========================================
# These are automatically set by Vercel during deployment
# Do not set manually unless testing locally
#
# NEXT_PUBLIC_VERCEL_TARGET_ENV=production|preview|development
# NEXT_PUBLIC_VERCEL_BRANCH_URL=your-branch-url.vercel.app
