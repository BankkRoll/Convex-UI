import {
  fetchAction,
  fetchMutation,
  fetchQuery,
  preloadQuery,
} from "convex/nextjs";

import { api } from "@/convex/_generated/api";

export { fetchQuery, fetchMutation, fetchAction, preloadQuery, api };
